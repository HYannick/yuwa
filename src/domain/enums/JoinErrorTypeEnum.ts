export enum JoinErrorTypeEnum {
  INVALID_TOKEN = 'invalid-token',
  EXPIRED_TOKEN = 'expired-token',
  MAX_USES_REACHED = 'max-uses-reached',
  ALREADY_JOINED = 'already-joined',
  FAILED_TO_JOIN = 'failed-to-join',
}